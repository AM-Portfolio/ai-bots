# =============================================================================
# AI Development Agent - Environment Configuration
# =============================================================================
#
# This file shows all available configuration options.
# Copy this to .env and fill in your values.
#

# =============================================================================
# PRIMARY LLM PROVIDER CONFIGURATION
# =============================================================================
# Choose your primary LLM provider: 'azure' or 'together'
# - Azure: Recommended for production, enterprise-ready
# - Together: Good for development, more model options
LLM_PROVIDER=azure

# =============================================================================
# AZURE OPENAI CONFIGURATION (Recommended for Production)
# =============================================================================
# Azure OpenAI Service credentials
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4
AZURE_OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-ada-002

# =============================================================================
# TOGETHER AI CONFIGURATION (Alternative Provider)
# =============================================================================
# Together AI credentials for fallback or primary use
TOGETHER_API_KEY=your_together_api_key_here
TOGETHER_MODEL=meta-llama/Llama-3.3-70B-Instruct-Turbo

# =============================================================================
# ROLE-BASED PROVIDER ASSIGNMENT
# =============================================================================
# Assign specific providers to different tasks
# Options: 'auto' (uses LLM_PROVIDER), 'azure', or 'together'
# 
# CHAT_PROVIDER: For chat and conversational responses
# EMBEDDING_PROVIDER: For vector embeddings and semantic search
# BEAUTIFY_PROVIDER: For response beautification and formatting
#
# Recommended setup:
# - Use Azure for chat, embeddings, and beautification (consistent quality)
# - Or mix providers based on your needs (e.g., Azure for chat, Together for embeddings)
CHAT_PROVIDER=auto
EMBEDDING_PROVIDER=auto
BEAUTIFY_PROVIDER=auto

# =============================================================================
# MULTILINGUAL SUPPORT
# =============================================================================
# Enable automatic language detection and multilingual responses
LANGUAGE_DETECTION_ENABLED=true
DEFAULT_LANGUAGE=en

# =============================================================================
# OPENAI CONFIGURATION (For Voice Features)
# =============================================================================
# OpenAI API key for Whisper STT and TTS
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_WHISPER_MODEL=whisper-1
OPENAI_TTS_MODEL=tts-1
OPENAI_TTS_VOICE=nova

# =============================================================================
# VOICE ASSISTANT SETTINGS
# =============================================================================
# Voice assistant configuration
VOICE_STT_PROVIDER=openai
VOICE_TTS_PROVIDER=openai
VOICE_PAUSE_DETECTION_MS=1500
VOICE_MAX_RECORDING_SECONDS=60
VOICE_RESPONSE_MAX_LENGTH=500

# =============================================================================
# GITHUB INTEGRATION
# =============================================================================
# GitHub personal access token with repo access
GITHUB_TOKEN=your_github_token_here
GITHUB_ORG=your-github-org

# =============================================================================
# JIRA INTEGRATION (Optional)
# =============================================================================
JIRA_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your_jira_api_token

# =============================================================================
# CONFLUENCE INTEGRATION (Optional)
# =============================================================================
CONFLUENCE_URL=https://your-domain.atlassian.net/wiki
CONFLUENCE_EMAIL=your-email@example.com
CONFLUENCE_API_TOKEN=your_confluence_api_token

# =============================================================================
# GRAFANA INTEGRATION (Optional)
# =============================================================================
GRAFANA_URL=https://your-grafana.com
GRAFANA_API_KEY=your-grafana-key

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database URL (provided by Replit)
# For local development, use SQLite: sqlite:///./ai_dev_agent.db
DATABASE_URL=postgresql://user:password@host:port/database

# =============================================================================
# VECTOR DATABASE CONFIGURATION
# =============================================================================
# Vector database provider: 'qdrant' or 'in-memory'
VECTOR_DB_PROVIDER=qdrant
QDRANT_HOST=localhost
QDRANT_PORT=6333
VECTOR_DB_FALLBACK_ENABLED=true

# =============================================================================
# AZURE CONFIGURATION (Optional - For Azure Services)
# =============================================================================
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret
AZURE_KEY_VAULT_URL=https://your-keyvault.vault.azure.net/

# Azure Speech Services (Alternative to OpenAI Whisper/TTS)
AZURE_SPEECH_KEY=your-azure-speech-key
AZURE_SPEECH_REGION=eastus
AZURE_SPEECH_LANG=en-US

# =============================================================================
# MICROSOFT TEAMS BOT (Optional)
# =============================================================================
MICROSOFT_APP_ID=your-app-id
MICROSOFT_APP_PASSWORD=your-app-password

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO
ENVIRONMENT=development

# =============================================================================
# EASY PROVIDER SWITCHING - Quick Setup Scenarios
# =============================================================================
#
# SCENARIO 1: Use Azure for Everything (Recommended for Production)
# ------------------------------------------------------------------
# LLM_PROVIDER=azure
# CHAT_PROVIDER=auto
# EMBEDDING_PROVIDER=auto
# BEAUTIFY_PROVIDER=auto
# → All features use Azure OpenAI for consistent quality and reliability
#
# SCENARIO 2: Use Together AI for Everything (Cost-Effective)
# ------------------------------------------------------------
# LLM_PROVIDER=together
# CHAT_PROVIDER=auto
# EMBEDDING_PROVIDER=auto
# BEAUTIFY_PROVIDER=auto
# → All features use Together AI (cheaper, good for development)
#
# SCENARIO 3: Mixed Providers - Azure Primary with Together Fallback
# -------------------------------------------------------------------
# LLM_PROVIDER=azure
# CHAT_PROVIDER=azure
# EMBEDDING_PROVIDER=azure
# BEAUTIFY_PROVIDER=together
# → Critical features (chat, embeddings) use Azure
# → Non-critical beautification uses Together to save costs
#
# SCENARIO 4: Mixed Providers - Together Primary with Azure Fallback
# -------------------------------------------------------------------
# LLM_PROVIDER=together
# CHAT_PROVIDER=together
# EMBEDDING_PROVIDER=azure
# BEAUTIFY_PROVIDER=together
# → Most features use Together (cost-effective)
# → Embeddings use Azure (better quality for semantic search)
#
# SCENARIO 5: All Azure with Automatic Fallback
# ----------------------------------------------
# LLM_PROVIDER=azure
# CHAT_PROVIDER=auto
# EMBEDDING_PROVIDER=auto
# BEAUTIFY_PROVIDER=auto
# → Primary: Azure OpenAI
# → Automatic fallback to Together AI if Azure fails
# → Best reliability with cost optimization
#
# =============================================================================
# QUICK START INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file: cp .env.example .env
#
# 2. For Azure Setup:
#    - Set LLM_PROVIDER=azure
#    - Fill in AZURE_OPENAI_ENDPOINT, AZURE_OPENAI_API_KEY
#    - Set AZURE_OPENAI_DEPLOYMENT_NAME to your deployment
#    - Set AZURE_OPENAI_EMBEDDING_DEPLOYMENT to your embedding model
#
# 3. For Together AI Setup:
#    - Set LLM_PROVIDER=together
#    - Fill in TOGETHER_API_KEY
#    - Optionally customize TOGETHER_MODEL
#
# 4. For Voice Features:
#    - Fill in OPENAI_API_KEY (for Whisper STT and TTS)
#    - Or use AZURE_SPEECH_KEY for Azure Speech Services
#
# 5. For GitHub Integration:
#    - Fill in GITHUB_TOKEN and GITHUB_ORG
#
# 6. Start the application:
#    - The system will automatically use your configured providers
#    - Fallback is enabled by default for reliability
#
# =============================================================================
